export enum MethodEnum {
  get = "GET",
  post = "POST",
  delete = "DELETE",
  put = "PUT",
}

export enum UrlFolderEnum {
  words = "words",
  users = "users",
  tokens = "tokens",
  aggregatedWords = "aggregatedWords",
  statistics = "statistics",
  settings = "settings",
  signin = "signin",
}